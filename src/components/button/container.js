import React from 'react';
import FileDrop from '../filedrop/filedrop';

const Container = props => (
    <div className='row' style={{padding: '0px 0px 100px 0px', margin: 'auto'}}> 
        <FileDrop />
    </div>
);

export default Container;